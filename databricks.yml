bundle:
  name: hanuman

include:
  - workflows/*.yml

targets:
  dev:
    workspace:
      host: https://adb-4497997910370043.3.azuredatabricks.net
      root_path: /Code/${bundle.name}
    sync:
      paths:
        - notebooks

